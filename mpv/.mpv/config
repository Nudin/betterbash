### Behavior ###
ontop
stop-screensaver
quiet
really-quiet

cache=auto
demuxer-max-bytes=500MiB
demuxer-max-back-bytes=500MiB
cache-pause-wait=5		# Cache 5s after 'buffering'
cache-pause-initial=yes

### Audio ###
#mixer-channel=Master
#audio-channels=6
# Fix A/V sync problems. *
mc=0.2
autosync=30
volume=80
# Max volume
#volume-max=100


### Video ###
#framedrop=decoder+vo # For slow machines
hwdec=auto-safe
vd-lavc-threads=3
#vo=opengl-hq

# Limit youtube-dl to max resolution of 1080p
ytdl-format='bestvideo[height<=?1080]+bestaudio/best[height<=?1080]'

### Subtitles ###
slang=de,ger,en,eng
sub-auto=all
osd-font=/usr/share/fonts/TTF/DejaVuSerifCondensed.ttf

sub-scale=0.5
#sub-pos="100"


### OSD ###
osd-scale=0.3
osd-bar-align-y=0.8
osd-bar-w=75
osd-bar-h=1.5
osd-border-size=0.7
osd-color='#D0FFFFFF'
# autocrop
script-opts-append=autocrop-auto=no
#script-opts-append=autocrop-auto_delay=5
script-opts-append=autocrop-detect_seconds=1
script-opts=webui-port=8000
